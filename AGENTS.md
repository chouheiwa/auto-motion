# 项目规则

- `PROMPT.md` 要像直接写给人类操作者的任务说明，不要暴露执行这份说明的是 agent、Codex 或 AI，也不要使用“主控 agent”这类身份设定；但可以明确说明他需要操作其他AI。

## 分支与内容边界

- `main` 只接受可跨项目复用的模板、工具、流程、风格规范和文档。
- 单片内容必须保存在独立分支或 worktree，不得合入 `main`。单片内容包括原稿、口播稿、输入清单、TTS 配置与证据、字幕、时间轴、分镜、镜头目录、音频素材、QC 报告、成片和发布文案。
- 从单片项目提炼到 `main` 的内容必须完成通用化：不得依赖某篇稿件、某个镜头编号、某次生成记录、单片目录或只在该项目中存在的证据文件。
- 合并前检查相对 `origin/main` 的新增文件，并运行 `auto-test/validate-main-boundary.sh`；发现单片内容时，保留在对应项目分支，只迁入已经解耦的通用资产。
